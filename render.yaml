services:
  - type: web
    name: sanket-watchlist
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: AUTH_TRUST_HOST
        value: "true"
